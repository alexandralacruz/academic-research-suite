[project]
name = "academic-research-suite"
version = "0.1.0"
description = ""
authors = [
    {name = "Alexandra La Cruz",email = "alexandra.lacruz@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "academic_research_suite", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
